apiVersion: v1
kind: Service
metadata:
  name: primary-app
  namespace: primary
spec:
  type: NodePort
  selector:
    app: primary-app
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080
